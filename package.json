{
  "name": "@freckle/non-empty",
  "version": "3.0.0",
  "type": "module",
  "description": "Construct and operate on non-empty arrays with type-safety",
  "main": "./dist/index.js",
  "repository": "https://github.com/freckle/non-empty-js.git",
  "author": "Freckle",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.build.json -d",
    "test": "vitest run",
    "check-esm": "node --input-type=module -e \"import './dist/index.js'\"",
    "format": "prettier --write 'src/**/*.ts'",
    "check-git-clean": "./check-git-clean.sh"
  },
  "dependencies": {
    "@freckle/maybe": "^3.0.0"
  },
  "devDependencies": {
    "expect-type": "^1.4.0",
    "jsdom": "^30.0.1",
    "prettier": "^3.9.6",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
